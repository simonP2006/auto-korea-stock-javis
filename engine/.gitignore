# 환경변수 및 시크릿
.env
*.key
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# IDE
.vscode/
.idea/
.cursor/

# 로그 및 데이터
logs/*.log
data/*.csv
data/*.parquet
data/*.feather

# 분석 결과 (개인 투자 기록이므로 Git 비공개)
reports/*.xlsx
reports/*.html
reports/*.png

# macOS
.DS_Store

# 백업 파일 (filter-tune 파라미터 튜닝 시 생성: *.bak.YYYYMMDD_HHmmss — TS-2)
*.bak.*
# reports: 대용량 스캔 데이터는 제외하되, 튜닝 핵심 자원은 추적 (주인님 지령 2026-06-13)
reports/*
!reports/*/
reports/*/*
!reports/*/masterReference.md
!reports/*/masterReference.log
!reports/tuning-log.md
# backfill 산출물(과거일 부분수집)은 실스캔 이력과 분리된 조회전용 임시 데이터 — 전체 제외
reports_backfill/
data/.token_cache.json

# Phase B 분석 산출물 — out/ 전체 재생성가능(recall_ab·full_universe_ab·fragility_sweep 등으로 재생성) → 제외, 스크립트만 추적
analyses/phase_b/out/
reports_eval/
src/**/*.bak.*
