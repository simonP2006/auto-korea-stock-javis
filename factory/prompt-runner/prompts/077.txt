[ABSOLUTE ANCHOR]
① 품질 절대우선 — 속도·토큰 비용 완전 무시. 품질·질적 수준이 절대 기준.
   sub agent vs agent-teams 선택 기준은 '품질'이지 '속도'가 아님.

② SOT + RLM 불변 — "기존 workflow 철학·목적·핵심 보존" +
   "단일 파일 SOT + 계층적 메모리 아래 다수 에이전트 동시 작동 시 데이터 불일치 없음" +
   RLM 패턴 절대 유지. SOT는 실제로 작동해야 한다.
   침범 가능성이 보이면 제안 전 먼저 보고.

③ 승인 후 구현 — 이 턴의 산출물은 "심층 성찰 보고서 + 권장 조치"뿐.
   /prompt/workflow-coding.md 본문 수정 금지. 실제 코드 구현 금지.
   승인 요청 시: 구현 의도(1문장) + 영향 파일 + 위험 지점 + 가정 사항.

→ 충돌 시 상위 기준 절대 우선.
[/ABSOLUTE ANCHOR]

[SESSION HEADER]
이전 단계: /prompt/workflow-coding.md 영어·번역 원칙 반영 완료.
이번 단계: 30년차 시니어 아키텍트 관점의 Critical Reflection.
          핵심 질문: 코드 작성·수정·업데이트·추가·삭제가 "의존성·결합도·변경 파급"을
          고려한 일관된 구현으로 설계되었는가?
[/SESSION HEADER]

[역할]
대규모 코드베이스 30년차 시니어 소프트웨어 엔지니어 겸 아키텍트.

[성찰 대상 — Infrastructure Build의 코드 구현 핵심 원칙]
아래 3원칙이 설계에 내부적으로 체계 수행되도록 반영되었는지 검증.

1. 사용자 지시의 의도 파악
   - 지시 내용을 한두 문장으로 명확히 설명 가능한가.
   - 변경 목적(버그·리팩토링·성능·기능)과 제약(호환성·스택)을 정확히 파악했는가.

2. 영향 범위 분석 (Dependency / Coupling / Ripple Effect)
   - 직접 의존: 수정 대상 함수/클래스/모듈/파일.
   - 호출 관계: 호출자·피호출자.
   - 구조적 관계: 상속·구현·합성·연관·참조.
   - 데이터 모델/DTO/스키마: 함께 바뀌어야 할 타입·필드·검증.
   - 테스트 코드: 단위/통합/스냅샷.
   - 설정/환경/빌드: config, DI, 라우팅, 의존성 주입.
   - 문서/주석/API 스펙.
   → "여기를 바꾸었으니 어디까지 파급되는가"를 전문가 수준으로 재조사·엄밀 평가.
   → 강결합·샷건 서저리 가능성은 미리 고지해 사용자와 협의.

3. 변경 설계 (Change Plan)
   - 단계별 변경 계획 제안: (1) 파일/클래스/함수부터 → (2) 하위 의존/호출자 전파 →
     (3) 테스트/문서/설정 맞춤.
   - "결합도 감소 / 응집도 증가" 관점의 리팩토링 기회도 함께 제안.

[출력 스타일]
이론 장황 설명 금지. 실질적 코드·구체 단계 위주. 중요한 설계 선택에는 간단한 이유.
모호해도 합리적 가정 명시 후 최선 구현 제안.

[성찰 품질 검증 — 3층위]
1층위 (사실 확인): 3원칙 각각이 Infrastructure Build 설계에 어떤 형태로 반영됐는지 매핑.
                   미반영 항목만 나열.
2층위 (구조 분석): 미반영·부분 반영이 구현 시 어떤 블로커가 되는가.
                   sub agent vs agent-teams 선택 기준이 "품질"로 되어 있는지 특히 점검.
3층위 (철학 정합성): SOT/RLM/철학 보존과 상충 없는가. 증거 기반.

[출력 형식]
A. 3원칙 ↔ 설계 매핑 표. B. 강결합·파급 위험 지점. C. 권장 조치 + 변경 설계 예시.
